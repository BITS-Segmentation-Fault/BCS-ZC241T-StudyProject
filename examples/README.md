# Examples

Build or install the `sandbox` executable as described in the repository
README, then run an example from the repository root:

```bash
sandbox --config examples/presentation/filesystem.yaml
```
